import { Container } from "@components/container";

const Cancel: React.FC = () => {
    return (
        <Container>
            Payment Cancel
        </Container>
    );
};

export default Cancel;
