import { Container } from "@components/container";

const Success: React.FC = () => {
    return (
        <Container>
            Payment Success
        </Container>
    );
};

export default Success;
